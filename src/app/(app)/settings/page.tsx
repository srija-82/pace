export default function SettingsPage() {
  return (
    <div style={{ padding: 24, color: "var(--text-tertiary)", fontSize: 13 }}>
      Settings — Account + Appearance cards go here.
    </div>
  );
}
